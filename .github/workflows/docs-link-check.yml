name: Docs Link Check

on:
  workflow_dispatch:

jobs:
  docs-link-check:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "TODO: implement docs link check workflow"

